{% extends 'base.html.twig' %}

{% block title %}Betaalmethode{% endblock %}

{% block body %}
    <h1>Betaalmethode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ betaalmethode.id }}</td>
            </tr>
            <tr>
                <th>Betaalwijze</th>
                <td>{{ betaalmethode.betaalwijze }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('betaalmethode_index') }}">back to list</a>

    <a href="{{ path('betaalmethode_edit', {'id': betaalmethode.id}) }}">edit</a>

    {{ include('betaalmethode/_delete_form.html.twig') }}
{% endblock %}
